<div>
    {{#Layout class="main_layout_feed"}}
        {{{ SideMenu inputs=searchForm.inputs}}}
        {{{ Feed inputs=messageForm.inputs }}}
    {{/Layout}}
    {{#with addChatForm}}
    {{{ ChatAddDialog
        inputs=inputs 
        formTitle=formTitle 
        formButton=formButton
        formName=formName
      }}}
    {{/with}}
    {{{ ChatEditDialog }}}
<div>
