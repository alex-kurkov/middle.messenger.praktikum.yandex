<input 
  class="input {{this.class}} text-paragraph-normal"
  type={{this.type}}
  {{#if value}}value={{this.value}}{{else}}value=""{{/if}}
  autocomplete={{this.autocomplete}}
  name={{this.name}}
/>

