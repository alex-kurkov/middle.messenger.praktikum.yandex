<form 
  class="form"
  action=""
  name=""  
  autocomplete=""
    >
    <fieldset class="form__fieldset text-paragraph-title">
    <legend class="form__legend">
      {{formTitle}}
    </legend>
      {{#each inputs}}
        {{> form-partials/input-field/input-field }}
      {{/each}}
    </fieldset>
    {{#if addon}}
      <div class="form__addon text-paragraph-normal">
        <span>{{addon.text}}</span>
        <a class="form__adon-link" href={{addon.link}}><span>{{addon.linkText}}</span></a>
      </div>
    {{/if}}
</form>